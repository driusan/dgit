package cmd

import (
	//"flag"
	//"fmt"
	//"os"
	//"strings"
	"github.com/driusan/dgit/git"
)

func SendPack(c *git.Client, args []string) {
	/*
		if toPost == "" {
			fmt.Fprintf(os.Stderr, "Already up to date\n")
			return refs, nil, NoNewCommits
		}
		r2, err := http.Post(s.location+"/git-receive-pack", "application/x-git-upload-pack-request", strings.NewReader(toPost))
		if err != nil {
			return refs, nil, err
		}
		defer r2.Body.Close()

	*/
}
